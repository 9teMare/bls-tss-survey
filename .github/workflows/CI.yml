name: CI
run-name: Running CI jobs 🚀
on: [push]
jobs:
  library:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository code
        uses: actions/checkout@v3
      - name: library tests
        run: |
          bash bls-tss-survey/scripts/testLib.sh
      - name: binance ECDSA
        run: |
          bash bls-tss-survey/scripts/testBinanceECDSA.sh
      - name: binance EDDSA
        run: |
          bash bls-tss-survey/scripts/testBinanceEDDSA.sh
      - name: integration tests
        run: |
          bash bls-tss-survey/scripts/testIntegration.sh

